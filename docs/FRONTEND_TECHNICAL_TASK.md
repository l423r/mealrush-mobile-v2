# Техническое задание: MealRush Mobile Application

**Версия:** 2.0.0  
**Дата создания:** 20 октября 2024  
**Статус:** Утверждено  
**Для:** Frontend разработки (Knois)

---

## 1. Введение

### 1.1. Контекст проекта

MealRush — мобильное приложение для отслеживания питания и подсчета калорий с акцентом на удобство использования и точность данных. Данное техническое задание описывает разработку мобильного приложения **с нуля** с использованием современного технологического стека.

**Важно:** Проект создается с чистого листа. Это не рефакторинг - это полная переписка с применением современных подходов React Native и Expo.

**Ссылка на общее ТЗ:** `TECHNICAL_TASK_MealRush.md`  
**Ссылка на Backend ТЗ:** `BACKEND_TECHNICAL_TASK.md`  
**Ссылка на API контракт:** `API_CONTRACT.md`

### 1.2. Цели Frontend

1. Создать интуитивное и быстрое мобильное приложение
2. Обеспечить отличный UX (время добавления приема пищи < 30 сек)
3. Реализовать все ключевые функции MVP
4. Обеспечить работу на Android 5.0+ и iOS 13+
5. Подготовить приложение к масштабированию

### 1.3. Границы ответственности

**Frontend отвечает за:**

- UI/UX всех экранов приложения
- Клиентскую валидацию данных
- Взаимодействие с Backend API
- Управление состоянием (MobX)
- Работу с камерой и галереей
- Локальное хранение токенов (Secure Storage)
- Адаптивность под разные размеры экранов

**Frontend НЕ отвечает за:**

- Серверную бизнес-логику
- Хранение данных в БД
- Интеграцию с внешними API (делает Backend)
- Генерацию JWT токенов

---

## 2. Технологический стек

### 2.1. Основные технологии

**Платформа:**

- React Native 0.74.5+
- Expo SDK ~51.0
- TypeScript (рекомендуется) или JavaScript

**Навигация:**

- React Navigation 6.x
  - Bottom Tabs Navigator
  - Native Stack Navigator

**Управление состоянием:**

- MobX 6.x
- MobX React Lite (для React интеграции)
- MobX Persist Store (для сохранения состояния)

**UI компоненты:**

- React Native Paper 5.x (Material Design)
- React Native Vector Icons
- Custom components

**Работа с API:**

- Axios или Fetch API
- Интерцепторы для токенов

**Камера и изображения:**

- Expo Camera
- Expo Image Picker
- Expo Barcode Scanner

**Хранилище:**

- Expo Secure Store (для токенов)
- AsyncStorage (для некритичных данных)

**Даты:**

- date-fns или Day.js (легковесная альтернатива Moment.js)
- @react-native-community/datetimepicker

**Уведомления:**

- Expo Notifications
- Firebase Cloud Messaging

**Утилиты:**

- React Hook Form (формы)
- Yup (валидация схем)
- lodash (при необходимости)

### 2.2. Сборка и публикация

**Development:**

- Expo Go (для быстрого тестирования)
- Dev Build (для нативных модулей)

**Production:**

- EAS Build (Expo Application Services)
- Android: APK/AAB
- iOS: IPA

**CI/CD:**

- GitHub Actions
- Автоматическая сборка при push в main

### 2.3. Тестирование

**Unit/Integration тесты:**

- Jest (included with React Native)
- React Native Testing Library
- Mock для API запросов

**E2E тесты (опционально, Фаза 2):**

- Detox

**Manual QA:**

- Тестирование на реальных устройствах
- Android 5.0+, iOS 13+
- Разные размеры экранов

### 2.4. Линтинг и форматирование

- ESLint (React Native config)
- Prettier
- Pre-commit hooks (Husky + lint-staged)

---

## 3. Архитектура приложения

### 3.1. Структура проекта

```
FoodApp/
├── src/
│   ├── api/                    # API клиенты и запросы
│   │   ├── axios.config.ts    # Axios instance с interceptors
│   │   ├── endpoints.ts       # API endpoints константы
│   │   └── services/          # API сервисы по модулям
│   │       ├── auth.service.ts
│   │       ├── profile.service.ts
│   │       ├── product.service.ts
│   │       ├── meal.service.ts
│   │       └── ...
│   │
│   ├── components/            # Переиспользуемые компоненты
│   │   ├── common/           # Общие компоненты
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Loading.tsx
│   │   │   └── ...
│   │   ├── meal/             # Компоненты для приемов пищи
│   │   ├── product/          # Компоненты для продуктов
│   │   └── profile/          # Компоненты для профиля
│   │
│   ├── screens/              # Экраны приложения
│   │   ├── auth/             # Экраны аутентификации
│   │   │   ├── SignInScreen.tsx
│   │   │   ├── onboarding/   # Онбординг экраны
│   │   │   └── ...
│   │   ├── main/             # Главные экраны
│   │   │   ├── MainScreen.tsx
│   │   │   ├── ProductsScreen.tsx
│   │   │   └── ProfileScreen.tsx
│   │   └── ...
│   │
│   ├── navigation/           # Навигация
│   │   ├── AppNavigator.tsx  # Главный навигатор
│   │   ├── AuthNavigator.tsx # Навигация для неавт. пользователей
│   │   └── MainNavigator.tsx # Навигация для авт. пользователей
│   │
│   ├── stores/               # MobX stores
│   │   ├── RootStore.ts      # Root store
│   │   ├── AuthStore.ts      # Аутентификация
│   │   ├── ProfileStore.ts   # Профиль пользователя
│   │   ├── ProductStore.ts   # Продукты
│   │   ├── MealStore.ts      # Приемы пищи
│   │   ├── UIStore.ts        # UI состояние
│   │   └── index.ts          # Store context
│   │
│   ├── hooks/                # Custom hooks
│   │   ├── useAuth.ts
│   │   ├── useApi.ts
│   │   └── ...
│   │
│   ├── utils/                # Утилиты
│   │   ├── validation.ts     # Валидация
│   │   ├── formatting.ts     # Форматирование
│   │   ├── calculations.ts   # Расчеты КБЖУ
│   │   └── constants.ts      # Константы
│   │
│   ├── types/                # TypeScript types/interfaces
│   │   ├── api.types.ts      # API типы
│   │   ├── models.types.ts   # Модели данных
│   │   └── navigation.types.ts
│   │
│   └── theme/                # Тема и стили
│       ├── colors.ts         # Цветовая палитра
│       ├── typography.ts     # Типографика
│       ├── spacing.ts        # Отступы
│       └── theme.ts          # Общая тема
│
├── assets/                   # Статические ресурсы
│   ├── images/
│   ├── icons/
│   └── fonts/ (опционально)
│
├── App.tsx                   # Entry point (Provider для MobX)
├── app.json                  # Expo config
├── eas.json                  # EAS Build config
├── package.json
├── tsconfig.json
└── ...
```

### 3.2. State Management (MobX)

**Root Store:**

```typescript
// RootStore.ts
import { makeAutoObservable } from 'mobx';
import AuthStore from './AuthStore';
import ProfileStore from './ProfileStore';
import ProductStore from './ProductStore';
import MealStore from './MealStore';
import UIStore from './UIStore';

class RootStore {
  authStore: AuthStore;
  profileStore: ProfileStore;
  productStore: ProductStore;
  mealStore: MealStore;
  uiStore: UIStore;

  constructor() {
    this.authStore = new AuthStore(this);
    this.profileStore = new ProfileStore(this);
    this.productStore = new ProductStore(this);
    this.mealStore = new MealStore(this);
    this.uiStore = new UIStore(this);
    makeAutoObservable(this);
  }
}

export default RootStore;
```

**Пример Store (AuthStore):**

```typescript
import { makeAutoObservable, runInAction } from 'mobx';
import { authService } from '../api/services/auth.service';

class AuthStore {
  rootStore: RootStore;
  token: string | null = null;
  isAuthenticated: boolean = false;
  user: User | null = null;
  loading: boolean = false;
  error: string | null = null;

  constructor(rootStore: RootStore) {
    this.rootStore = rootStore;
    makeAutoObservable(this);
  }

  async login(email: string, password: string) {
    this.loading = true;
    this.error = null;
    try {
      const response = await authService.login(email, password);
      runInAction(() => {
        this.token = response.data.jwt_token;
        this.isAuthenticated = true;
        this.loading = false;
      });
      await this.saveToken(response.data.jwt_token);
    } catch (error) {
      runInAction(() => {
        this.error = error.message;
        this.loading = false;
      });
    }
  }

  logout() {
    this.token = null;
    this.isAuthenticated = false;
    this.user = null;
    this.deleteToken();
  }
}

export default AuthStore;
```

**MobX patterns:**

- makeAutoObservable для автоматической обсерваемости
- runInAction для мутаций после async
- Computed values для производных данных
- Reactions для side effects
- makePersistable для персистентности (mobx-persist-store)

**Использование в компонентах:**

```typescript
// MainScreen.tsx
import { observer } from 'mobx-react-lite';
import { useStores } from '../stores';

const MainScreen = observer(() => {
  const { mealStore, profileStore } = useStores();

  useEffect(() => {
    mealStore.loadMealsForDate(selectedDate);
  }, [mealStore.selectedDate]);

  return (
    <View>
      <Text>Калории: {mealStore.dailyCalories} / {profileStore.dayLimitCal}</Text>
      <FlatList
        data={mealStore.meals}
        renderItem={({ item }) => <MealCard meal={item} />}
      />
    </View>
  );
});

export default MainScreen;
```

**Store Context:**

```typescript
// stores/index.ts
import React from 'react';
import RootStore from './RootStore';

const StoreContext = React.createContext<RootStore | null>(null);

export const StoreProvider = ({ children }: { children: React.ReactNode }) => {
  const rootStore = React.useMemo(() => new RootStore(), []);
  return <StoreContext.Provider value={rootStore}>{children}</StoreContext.Provider>;
};

export const useStores = () => {
  const store = React.useContext(StoreContext);
  if (!store) throw new Error('useStores must be used within StoreProvider');
  return store;
};
```

### 3.3. API интеграция

**Axios configuration:**

```typescript
// axios.config.ts
import axios from 'axios';
import { getToken } from './secureStore';

const api = axios.create({
  baseURL: 'http://80.87.201.75:8079/gateway/my-food',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Request interceptor: добавление JWT токена
api.interceptors.request.use(async (config) => {
  const token = await getToken();
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// Response interceptor: обработка ошибок
api.interceptors.response.use(
  (response) => response,
  async (error) => {
    if (error.response?.status === 401) {
      // Redirect to login
    }
    return Promise.reject(error);
  }
);

export default api;
```

**Service pattern:**

```typescript
// product.service.ts
import api from '../axios.config';

export const productService = {
  getAll: (page = 0, size = 20) =>
    api.get('/product', { params: { page, size } }),

  searchByName: (name: string, page = 0, size = 20) =>
    api.get('/product/search/name', { params: { name, page, size } }),

  searchByBarcode: (barcode: string) =>
    api.get(`/product/search/barcode/${barcode}`),

  create: (product: ProductCreate) => api.post('/product', product),

  update: (product: ProductUpdate) => api.put('/product', product),

  delete: (id: number) => api.delete(`/product/${id}`),
};
```

---

## 4. Функциональные требования (Экраны и функции)

### 4.1. Модуль: Аутентификация

#### Экран: SignInScreen

**Элементы:**

- Логотип приложения
- Input: Email
- Input: Пароль (с иконкой показать/скрыть)
- Button: "Войти"
- Button: "Войти через Google"
- Link: "Забыли пароль?"
- Link: "Зарегистрироваться"

**Валидация:**

- Email: корректный формат
- Пароль: минимум 8 символов
- Показать ошибки под полями

**API:**

- `POST /auth/token` → получение JWT
- Сохранение токена в Secure Store
- Навигация на MainScreen после успеха

#### Экран: Онбординг (6-7 экранов)

**Экраны:**

1. GetGenderScreen - выбор пола (MALE/FEMALE)
2. GetTargetScreen - выбор цели (LOSE/SAVE/GAIN)
3. GetWeightScreen - ввод текущего веса
4. GetTargetWeightScreen - ввод желаемого веса (если цель не SAVE)
5. GetHeightScreen - ввод роста
6. GetBirthdayScreen - выбор даты рождения
7. GetActivityScreen - выбор уровня активности (5 вариантов)
8. RegistrationScreen - ввод email, пароля, имени

**UI/UX:**

- Прогресс-бар вверху экрана
- Кнопка "Назад" (кроме первого экрана)
- Кнопка "Далее" (disabled если не заполнено)
- Анимации переходов

**API:**

- `POST /auth/user` → регистрация пользователя
- `POST /my-food/user-profile` → создание профиля
- Автоматический вход после регистрации

### 4.2. Модуль: Главный экран (Расписание питания)

#### Экран: MainScreen

**Элементы:**

- Header: "Расписание питания" + иконка календаря
- Выбранная дата (день недели, дата)
- Блок дневной статистики КБЖУ (прогресс-бары)
- Список приемов пищи за день (FlatList)
- Button: "Добавить прием пищи" (плавающая кнопка)

**Дневная статистика:**

```
Калории: 1450 / 1800 kcal [прогресс-бар 80%]
Белки: 85 / 100 г [прогресс-бар]
Жиры: 50 / 60 г [прогресс-бар]
Углеводы: 180 / 200 г [прогресс-бар]
```

**Карточка приема пищи:**

- Иконка типа приема (завтрак/обед/ужин)
- Время
- Список блюд (краткий)
- Суммарные КБЖУ

**Взаимодействие:**

- Нажатие на календарь → открытие date picker
- Нажатие на прием пищи → переход к MealScreen
- Нажатие "Добавить" → переход к SearchScreen

**API:**

- `GET /meal/findByDate?date={YYYY-MM-DD}` → приемы пищи за дату
- `GET /meal_element/meal/{mealId}` → элементы каждого приема

**Расчет статистики:**

- Суммирование КБЖУ всех meal_elements за день
- Сравнение с day_limit_cal из профиля

### 4.3. Модуль: Поиск и добавление продуктов

#### Экран: SearchScreen

**Элементы:**

- Header: "Поиск" + кнопка назад + иконка сканера
- Tabs: "Все" | "Избранное"
- Search Input (с debounce 300ms)
- Список продуктов (FlatList)
- Button: "Создать новый продукт"

**Карточка продукта:**

- Фото (или placeholder)
- Название
- КБЖУ на 100г
- Иконка "избранное" (звездочка)

**Взаимодействие:**

- Ввод в поиск → API запрос → обновление списка
- Нажатие на продукт → переход к MealElementScreen
- Нажатие на звездочку → добавить/удалить из избранного
- Нажатие на сканер → переход к ScannerScreen
- Нажатие "Создать" → переход к ProductScreen

**API:**

- `GET /product/search/name?name={query}` → поиск продуктов
- `GET /favorite` → список избранных
- `POST /favorite/{productId}` → добавить в избранное
- `DELETE /favorite/{productId}` → удалить из избранного

#### Экран: ScannerScreen

**Элементы:**

- Header: "Сканер штрихкодов" + кнопка назад
- Camera view с рамкой для сканирования
- Индикатор загрузки при обработке
- Инструкция "Наведите камеру на штрихкод"

**Логика:**

- Запрос разрешения на камеру
- Сканирование штрихкода (EAN-13, UPC)
- Отправка в Backend API
- При успехе → переход к MealElementScreen с данными продукта
- При ошибке → показать сообщение, возможность повторить

**API:**

- `GET /product/search/barcode/{barcode}` → поиск по штрихкоду

#### Экран: ProductScreen (создание/редактирование)

**Элементы:**

- Header: "Создание продукта" / "Редактирование продукта"
- Input: Название
- ImagePicker: Фото продукта (опционально)
- Input: Белки (на 100г)
- Input: Жиры (на 100г)
- Input: Углеводы (на 100г)
- Input: Калории (автоматический расчет или ручной ввод)
- Input: Количество (100г по умолчанию)
- Picker: Категория продукта
- Checkbox: "Добавить в избранное"
- Button: "Сохранить"
- Hint: "При сохранении все показатели будут пересчитаны на 100г"

**Валидация:**

- Название: не пустое
- Количество: > 0
- КБЖУ: числа >= 0

**API:**

- `POST /product` → создание продукта
- `PUT /product` → обновление продукта
- `GET /product_category` → список категорий

### 4.4. Модуль: Создание приема пищи

#### Экран: MealElementScreen

**Элементы:**

- Header: "Блюдо" + кнопка назад + иконка корзины (если редактирование)
- Название продукта/блюда (большой текст)
- ImageView: Фото блюда (если есть)
- Display/Input: Белки, Жиры, Углеводы, Калории (на указанное кол-во)
- Input: Количество (граммы)
- Picker: Тип приема пищи (если новый прием)
- TimePicker: Время (если новый прием)
- Checkbox: "Добавить в базу продуктов" (если новый продукт)
- Button: "Сохранить" / "ОК"

**Логика:**

- Если продукт из поиска → показать данные, редактировать только количество
- При изменении количества → автоматический пересчет КБЖУ
- Формула: `actual_value = (base_value_per_100g / 100) * quantity`

**API:**

- `POST /meal` → создание приема пищи (если еще нет)
- `POST /meal_element` → создание элемента приема пищи
- `PUT /meal_element` → обновление элемента
- `DELETE /meal_element/{id}` → удаление элемента

#### Экран: MealScreen

**Элементы:**

- Header: Тип приема + время + иконка редактирования
- Список элементов (блюд) в приеме пищи
- Суммарные КБЖУ приема пищи
- Button: "Добавить блюдо"

**Взаимодействие:**

- Нажатие на блюдо → редактирование MealElementScreen
- Нажатие "Добавить" → переход к SearchScreen с передачей meal_id

### 4.5. Модуль: Анализ по фото (NEW)

#### Flow: Анализ блюда по фото

**Точка входа:** Кнопка "Анализ по фото" на SearchScreen

**Шаги:**

1. **Выбор источника:**
   - Modal: "Сделать фото" | "Выбрать из галереи"
   - Запрос разрешений

2. **Выбор фото:**
   - Если "Сделать фото" → открыть камеру
   - Если "Галерея" → Image Picker
   - Preview выбранного фото

3. **Отправка на анализ:**
   - Конвертация фото в base64
   - Отправка в Backend API
   - Показать Loading с текстом "Анализируем блюдо..." (до 40 сек)

4. **Результаты:**
   - Показать список распознанных ингредиентов
   - Для каждого: название, количество, КБЖУ
   - Общие КБЖУ блюда
   - Возможность корректировать

5. **Сохранение:**
   - Выбор типа приема пищи и времени
   - Создание meal и meal_element с фото
   - Возврат на MainScreen

**API:**

- `POST /meal_element/analyze-photo` → анализ фото
  - Request: `{ "image_base64": "...", "prompt": "..." }`
  - Response: `{ "ingredients": [{name, quantity, proteins, fats, carbs, calories}], "total_nutrients": {...} }`

**Обработка ошибок:**

- Timeout → показать сообщение, предложить повторить или ввести вручную
- API error → предложить создать блюдо вручную
- Плохое фото → подсказка сделать фото получше

### 4.6. Модуль: Профиль

#### Экран: ProfileScreen

**Элементы:**

- Header: "Профиль" + иконка настроек
- Аватар (мужчина/женщина)
- Имя, Email
- Блок "Текущие показатели"
  - Вес, Рост, Возраст
- Блок "Цель"
  - Тип цели (набрать/сбросить/сохранить)
  - Желаемый вес
- Блок "Активность"
  - Уровень физической активности
- Блок "Калорийность"
  - Ежедневный лимит (установленный)
  - Рекомендованный лимит (расчетный)
  - Суточная норма
- ИМТ (Индекс Массы Тела)
- Button: "Редактировать"

**API:**

- `GET /user-profile` → получение профиля
- Расчет ИМТ локально: `weight / (height/100)^2`

#### Экран: ProfileEditScreen

**Элементы:**

- Header: "Редактирование профиля"
- Input: Вес
- Input: Рост
- Input: Дата рождения (DatePicker)
- Picker: Цель
- Input: Желаемый вес (если цель не SAVE)
- Picker: Уровень активности
- Input: Ежедневный лимит калорий
- Button: "Сохранить"

**API:**

- `PUT /user-profile` → обновление профиля
- Backend пересчитывает рекомендации

#### Экран: SettingsScreen

**Элементы:**

- Header: "Настройки"
- Option: "Изменить имя"
- Option: "Изменить email"
- Option: "Изменить пароль"
- Option: "Удалить аккаунт" (красная)
- Button: "Выйти"

**Переходы:**

- SettingsNameScreen
- SettingsEmailScreen
- SettingsPasswordScreen
- SettingsDeleteAccountScreen

---

## 5. Нефункциональные требования

### 5.1. Производительность

**Целевые показатели:**

- Холодный старт приложения: < 3 сек
- Теплый старт: < 1 сек
- Переходы между экранами: < 300 мс
- Время добавления приема пищи: < 30 сек (UX цель)
- Плавность скролла: 60 FPS

**Оптимизации:**

- FlatList для больших списков
- React.memo для дорогих компонентов
- useMemo, useCallback где нужно
- Оптимизация изображений (сжатие, lazy loading)
- Debounce для поисковых запросов (300ms)
- Pagination для списков

### 5.2. UX/UI

**Принципы:**

- Минимализм и простота
- Последовательность в дизайне
- Быстрая обратная связь (Loading indicators, Snackbars)
- Интуитивность без инструкций
- Адаптивность под разные размеры экранов

**Темная/Светлая тема:**

- Автоматическое переключение по системным настройкам
- Сохранение предпочтений пользователя (опционально)

**Цветовая схема:**

- Основной: #43a047 (зеленый)
- Серый текст: #9599a4
- Фон светлый: #f2f2f2
- Фон темный: #222222
- Ошибка: #BA1A1A

**Типографика:**

- Заголовки: Bold, 20-24px
- Основной текст: Regular, 14-16px
- Второстепенный: Regular, 12-14px

### 5.3. Совместимость

**Платформы:**

- Android 5.0+ (API 21)
- iOS 13.0+

**Размеры экранов:**

- От 4.7" до 6.7"+
- Portrait ориентация (основная)
- Landscape не требуется

**Тестирование на устройствах:**

- Android: разные производители (Samsung, Xiaomi, Google)
- iOS: разные модели iPhone (SE, 13, 14)

### 5.4. Безопасность

**Хранение токенов:**

- Expo Secure Store для JWT токенов
- Никогда не логировать токены в консоль (в production)

**Валидация:**

- Клиентская валидация перед отправкой
- Санитизация пользовательского ввода
- Максимальные длины полей

**HTTPS:**

- Все запросы через HTTPS (в production)

### 5.5. Офлайн режим

**MVP:** Нет офлайн режима

**Фаза 2:**

- Кеширование данных с MobX Persist Store
- Синхронизация при восстановлении соединения

---

## 6. Навигация

### 6.1. Структура навигации

```
Root Navigator (Native Stack)
│
├─ AuthNavigator (Native Stack)
│  ├─ LoadingScreen
│  ├─ SignInScreen
│  ├─ GetGenderScreen
│  ├─ GetTargetScreen
│  ├─ GetWeightScreen
│  ├─ GetTargetWeightScreen
│  ├─ GetHeightScreen
│  ├─ GetBirthdayScreen
│  ├─ GetActivityScreen
│  ├─ RegistrationScreen
│  └─ RulesScreen
│
└─ MainNavigator (Native Stack)
   ├─ HomeTabs (Bottom Tabs)
   │  ├─ MainScreen (Приемы пищи)
   │  ├─ ProductTabsScreen (База продуктов)
   │  │  ├─ AllProductsTab
   │  │  └─ MyProductsTab
   │  └─ ProfileScreen (Профиль)
   │
   ├─ SearchScreen
   ├─ ScannerScreen
   ├─ ProductScreen
   ├─ MealScreen
   ├─ MealElementScreen
   ├─ ProfileEditScreen
   ├─ SettingsScreen
   ├─ SettingsNameScreen
   ├─ SettingsEmailScreen
   ├─ SettingsPasswordScreen
   └─ SettingsDeleteAccountScreen
```

### 6.2. Навигационные параметры

**TypeScript типы:**

```typescript
type RootStackParamList = {
  Auth: undefined;
  Main: undefined;
};

type AuthStackParamList = {
  SignIn: undefined;
  GetGender: undefined;
  // ...
};

type MainStackParamList = {
  HomeTabs: undefined;
  Search: { date?: string; mealId?: number };
  Scanner: { date?: string; mealId?: number };
  MealElement: {
    item?: Product | MealElement;
    date?: string;
    mealId?: number;
    fromSearch?: boolean;
  };
  Product: { product?: Product };
  // ...
};
```

### 6.3. Deep linking

**Схема:** `mealrush://`

**Маршруты:**

- `mealrush://main` → MainScreen
- `mealrush://search` → SearchScreen
- `mealrush://profile` → ProfileScreen

---

## 7. Задачи MVP (Приоритизированный список)

### Фаза 1: MVP - 1 месяц

#### P0 - Критично (Неделя 1)

**Инфраструктура:**

- [ ] Инициализация Expo проекта (TypeScript)
- [ ] Настройка navigation (React Navigation)
- [ ] Настройка MobX + MobX React Lite
- [ ] Настройка MobX Persist Store
- [ ] Настройка Axios + interceptors
- [ ] Конфигурация ESLint + Prettier
- [ ] Базовая структура папок

**Аутентификация:**

- [ ] SignInScreen (UI + API интеграция)
- [ ] Сохранение/чтение токена (Secure Store)
- [ ] Auto-login при наличии токена
- [ ] Logout функциональность

**Онбординг:**

- [ ] GetGenderScreen
- [ ] GetTargetScreen
- [ ] GetWeightScreen
- [ ] GetTargetWeightScreen
- [ ] GetHeightScreen
- [ ] GetBirthdayScreen
- [ ] GetActivityScreen
- [ ] RegistrationScreen
- [ ] Прогресс-бар и навигация между экранами

**Компоненты:**

- [ ] Button компонент
- [ ] Input компонент
- [ ] Loading компонент
- [ ] Header компонент

#### P1 - Высокий приоритет (Неделя 1-2)

**Главный экран:**

- [ ] MainScreen (UI layout)
- [ ] Date picker для выбора даты
- [ ] Список приемов пищи (FlatList)
- [ ] Дневная статистика КБЖУ (прогресс-бары)
- [ ] API: получение приемов пищи за дату
- [ ] Расчет суммарных КБЖУ

**Поиск продуктов:**

- [ ] SearchScreen (UI + tabs)
- [ ] Search Input с debounce
- [ ] Список продуктов (FlatList)
- [ ] API: поиск продуктов по имени
- [ ] API: избранные продукты
- [ ] Добавление/удаление из избранного

**Создание элемента:**

- [ ] MealElementScreen (UI)
- [ ] Пересчет КБЖУ при изменении количества
- [ ] Выбор типа приема пищи и времени
- [ ] API: создание meal + meal_element
- [ ] Возврат на MainScreen и обновление

**Профиль:**

- [ ] ProfileScreen (UI)
- [ ] API: получение профиля
- [ ] Отображение данных и расчетов

#### P2 - Средний приоритет (Неделя 2-3)

**Сканер штрихкодов:**

- [ ] ScannerScreen (Camera + Barcode Scanner)
- [ ] Запрос разрешений на камеру
- [ ] API: поиск по штрихкоду
- [ ] Обработка результатов сканирования
- [ ] Обработка ошибок (не найден, невалидный код)

**Продукты:**

- [ ] ProductScreen (создание/редактирование)
- [ ] Image Picker для фото продукта
- [ ] Валидация полей
- [ ] API: создание/обновление продукта
- [ ] Конвертация base64 изображений

**Профиль:**

- [ ] ProfileEditScreen (редактирование)
- [ ] API: обновление профиля
- [ ] Валидация данных

**Настройки:**

- [ ] SettingsScreen (список опций)
- [ ] SettingsNameScreen, SettingsEmailScreen, SettingsPasswordScreen
- [ ] SettingsDeleteAccountScreen
- [ ] API интеграция для каждого экрана

#### P3 - Низкий приоритет (Неделя 3-4)

**Анализ по фото:**

- [ ] Flow выбора фото (камера/галерея)
- [ ] Modal для выбора источника
- [ ] Конвертация фото в base64
- [ ] API: отправка фото на анализ
- [ ] Экран результатов анализа
- [ ] Корректировка и сохранение результатов
- [ ] Обработка ошибок и таймаутов

**Google Sign-In:**

- [ ] Google Sign-In button
- [ ] Firebase/Google OAuth интеграция
- [ ] Получение токена от Backend
- [ ] Обработка ошибок

**UI/UX улучшения:**

- [ ] Темная/светлая тема (автопереключение)
- [ ] Snackbar компонент для уведомлений
- [ ] Анимации переходов
- [ ] Skeleton loaders
- [ ] Empty states (нет данных)

**Push уведомления:**

- [ ] Запрос разрешений на уведомления
- [ ] Получение device token
- [ ] API: регистрация устройства
- [ ] Обработка входящих уведомлений

#### Тестирование и оптимизация:

- [ ] Unit тесты для утилит (валидация, расчеты)
- [ ] Integration тесты для ключевых компонентов
- [ ] Тестирование на Android устройствах
- [ ] Тестирование на iOS устройствах
- [ ] Оптимизация производительности (профилирование)
- [ ] Исправление багов

#### Сборка:

- [ ] Конфигурация EAS Build
- [ ] Test build для Android
- [ ] Test build для iOS
- [ ] Internal testing (TestFlight, Google Play Internal)

### Критерии приемки MVP:

✅ Все P0, P1 и P2 задачи выполнены  
✅ Пользователь может зарегистрироваться и войти  
✅ Пользователь может добавлять приемы пищи (поиск, сканер)  
✅ Пользователь может видеть статистику за день  
✅ Пользователь может редактировать профиль  
✅ Время добавления приема пищи < 30 сек  
✅ Приложение работает на Android 5.0+ и iOS 13+  
✅ Темная/светлая тема работает  
✅ Нет критичных багов  
✅ Build успешно создается через EAS

---

## 8. Тестирование

### 8.1. Unit тесты

**Тестировать:**

- Утилиты валидации
- Утилиты расчетов (КБЖУ, ИМТ)
- Утилиты форматирования (даты, числа)
- MobX stores и actions

**Пример:**

```typescript
// calculations.test.ts
describe('calculateCalories', () => {
  it('should calculate calories correctly', () => {
    const result = calculateCalories(10, 5, 20); // proteins, fats, carbs
    expect(result).toBe(165); // (10*4) + (5*9) + (20*4)
  });
});
```

### 8.2. Integration тесты

**Тестировать:**

- Ключевые компоненты с API mock
- MobX flow (action → API → update observable state)
- Navigation flow

**Пример:**

```typescript
// MealElementScreen.test.tsx
describe('MealElementScreen', () => {
  it('should update calories when quantity changes', () => {
    const { getByTestId } = render(<MealElementScreen />);

    // Given
    const quantityInput = getByTestId('quantity-input');

    // When
    fireEvent.changeText(quantityInput, '200');

    // Then
    const caloriesDisplay = getByTestId('calories-display');
    expect(caloriesDisplay.props.children).toBe('200'); // doubled
  });
});
```

### 8.3. Manual QA

**Тестовые сценарии:**

- [ ] Регистрация и вход (happy path)
- [ ] Добавление приема пищи через поиск
- [ ] Добавление приема пищи через сканер
- [ ] Редактирование профиля
- [ ] Избранное (добавить/удалить)
- [ ] Переключение темы
- [ ] Работа при плохом интернете
- [ ] Работа на разных устройствах

**Checklist для релиза:**

- [ ] Все функции MVP работают
- [ ] Нет критичных багов
- [ ] Производительность приемлема
- [ ] UI/UX соответствует дизайну
- [ ] Приложение не крашится

---

## 9. CI/CD (GitHub Actions)

### 9.1. Workflow: Lint and Test

```yaml
name: Frontend CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run tests
        run: npm test -- --coverage

      - name: Upload coverage
        uses: codecov/codecov-action@v3
```

### 9.2. Workflow: EAS Build

```yaml
name: EAS Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Setup Expo
        uses: expo/expo-github-action@v7
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Build Android
        run: eas build --platform android --non-interactive

      - name: Build iOS
        run: eas build --platform ios --non-interactive
```

---

## 10. Конфигурация

### 10.1. app.json

```json
{
  "expo": {
    "name": "MealRush",
    "slug": "mealrush",
    "version": "2.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "automatic",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#43a047"
    },
    "updates": {
      "fallbackToCacheTimeout": 0,
      "url": "https://u.expo.dev/..."
    },
    "assetBundlePatterns": ["**/*"],
    "ios": {
      "supportsTablet": false,
      "bundleIdentifier": "com.mealrush.app",
      "infoPlist": {
        "NSCameraUsageDescription": "Для сканирования штрихкодов и анализа блюд",
        "NSPhotoLibraryUsageDescription": "Для добавления фото продуктов и блюд"
      }
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#43a047"
      },
      "package": "com.mealrush.app",
      "permissions": [
        "android.permission.CAMERA",
        "android.permission.READ_EXTERNAL_STORAGE"
      ]
    },
    "plugins": [
      "expo-camera",
      "expo-image-picker",
      "expo-barcode-scanner",
      "expo-secure-store"
    ]
  }
}
```

### 10.2. eas.json

```json
{
  "cli": {
    "version": ">= 5.0.0"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "app-bundle"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
```

### 10.3. Environment variables

**Для локальной разработки (.env):**

```bash
API_BASE_URL=http://80.87.201.75:8079/gateway/my-food
AUTH_BASE_URL=http://80.87.201.75:8079/gateway/auth
```

**Для production (через EAS):**

```bash
eas secret:create --name API_BASE_URL --value https://api.mealrush.com/my-food
```

---

## 11. Дополнительные рекомендации

### 11.1. Best Practices

**Код:**

- Использовать TypeScript для type safety
- Использовать functional components (hooks)
- Избегать inline styles (использовать StyleSheet)
- Observer HOC или observer hook для MobX компонентов
- Мemoization для оптимизации (React.memo, useMemo, useCallback)
- Custom hooks для переиспользуемой логики
- Использовать computed для производных значений

**Git:**

- Conventional commits (feat:, fix:, refactor:, etc.)
- Feature branches
- Pull requests с review

**UX:**

- Loading states для всех асинхронных операций
- Error states с понятными сообщениями
- Empty states когда нет данных
- Оптимистичные обновления где возможно

### 11.2. Потенциальные проблемы

**Проблема:** Медленная загрузка списков  
**Решение:** FlatList с windowSize оптимизацией, pagination

**Проблема:** Большие изображения  
**Решение:** Сжатие перед отправкой, lazy loading, caching

**Проблема:** Медленный поиск  
**Решение:** Debounce, кеширование результатов

**Проблема:** Сложность state management  
**Решение:** Модульная структура MobX stores, separation of concerns, использование RootStore

---

## 12. Ссылки и ресурсы

**Документация:**

- React Native: https://reactnative.dev/
- Expo: https://docs.expo.dev/
- React Navigation: https://reactnavigation.org/
- MobX: https://mobx.js.org/
- MobX React Lite: https://github.com/mobxjs/mobx-react-lite
- React Native Paper: https://callstack.github.io/react-native-paper/

**Design:**

- Material Design: https://m3.material.io/
- iOS Human Interface Guidelines: https://developer.apple.com/design/human-interface-guidelines/

**API:**

- См. `API_CONTRACT.md` (детальная документация)
- Backend Swagger: http://80.87.201.75:8079/gateway/my-food/swagger-ui/

---

## 13. Контакты и синхронизация

**Разработчик:** Knois  
**Роль:** Frontend Developer

**Связь с Backend:**

- Backend разработчик: L423r
- API контракт: `API_CONTRACT.md`
- Синхронизация: регулярные встречи
- Вопросы: через Issues или напрямую

**Документы для синхронизации:**

- Общее ТЗ: `TECHNICAL_TASK_MealRush.md`
- Backend ТЗ: `BACKEND_TECHNICAL_TASK.md`
- API контракт: `API_CONTRACT.md`

---

**Дата утверждения:** 20 октября 2024  
**Версия документа:** 1.0  
**Статус:** Готов к разработке 🚀
