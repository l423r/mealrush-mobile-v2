# E2E Тесты для MealRush Mobile

Автотесты для мобильного приложения MealRush, написанные на Python с использованием Appium.

## 📋 Быстрый старт

### 1. Установите зависимости

```bash
# Python пакеты
cd e2e_tests
pip install -r requirements.txt

# Appium (глобально)
npm install -g appium

# Драйвер для Android
appium driver install uiautomator2
```

### 2. Настройте окружение

Создайте файл `e2e_tests/.env` на основе `.env.example`:

```bash
cd e2e_tests
copy .env.example .env  # Windows
# или
cp .env.example .env    # Linux/Mac
```

Отредактируйте `.env` и укажите путь к вашему APK.

### 3. Соберите приложение

```bash
# Сборка debug APK
cd android
gradlew assembleDebug
```

### 4. Запустите тесты

```bash
# Запустите Appium в одном терминале
appium

# В другом терминале запустите тесты
cd e2e_tests

# Windows:
run_tests.bat

# Linux/Mac:
./run_tests.sh
```

## 📁 Структура проекта

```
e2e_tests/
├── config/              # Конфигурация Appium
├── pages/              # Page Objects (SignIn, Main, Search, Profile, etc.)
├── utilities/          # Базовые утилиты
├── tests/              # Тесты (authentication, main features, profile)
├── screenshots/        # Скриншоты
└── README.md           # Подробная документация
```

## 🧪 Запуск конкретных тестов

```bash
# Все тесты
pytest

# Только smoke тесты
pytest -m smoke

# Только authentication тесты
pytest tests/test_authentication.py

# С подробным выводом
pytest -v -s

# С HTML отчетом
pytest --html=report.html
```

## 📖 Подробная документация

См. файлы:

- `e2e_tests/README.md` - полная документация
- `e2e_tests/INSTRUCTION_RU.md` - подробная инструкция на русском

## ✅ Покрытие тестами

### Аутентификация

- ✅ Вход в систему
- ✅ Регистрация нового пользователя
- ✅ Валидация форм
- ✅ Переключение видимости пароля

### Основные функции

- ✅ Навигация между экранами
- ✅ Изменение даты
- ✅ Отображение статистики
- ✅ Добавление приемов пищи

### Поиск

- ✅ Поиск продуктов
- ✅ Отображение результатов
- ✅ Работа с избранным

### Профиль

- ✅ Просмотр информации о пользователе
- ✅ Отображение BMI
- ✅ Цели по калориям
- ✅ Переход в настройки

## 🔧 Требования

- Python 3.8+
- Node.js и npm
- Android SDK (для Android тестов)
- Appium 2.x
- Appium UIAutomator2 Driver

## ⚠️ Устранение неполадок

### Устройство не определяется

```bash
adb devices
adb kill-server
adb start-server
```

### Элементы не находятся

Используйте Appium Inspector:

```bash
appium inspector
```

Откройте http://localhost:4723 и создайте сессию для нахождения элементов.

### Подробные логи

```bash
appium --log-level debug
```

## 📊 Результаты

После запуска тестов:

- HTML отчет: `report.html`
- Скриншоты: `screenshots/`

## 🔗 Полезные ссылки

- [Appium Documentation](http://appium.io/docs/)
- [Pytest Documentation](https://docs.pytest.org/)
- [Selenium Documentation](https://www.selenium.dev/documentation/)
