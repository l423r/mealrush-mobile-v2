# Appium Configuration
APPIUM_SERVER_URL=http://localhost:4723/wd/hub
PLATFORM=android

# Android Configuration для ФИЗИЧЕСКОГО УСТРОЙСТВА
# Для эмулятора можно оставить "Android Emulator"
# Для физического устройства укажите уникальное имя устройства
ANDROID_PLATFORM_VERSION=15
ANDROID_DEVICE_NAME=53736f48
ANDROID_APP_PATH=../android/app/build/outputs/apk/debug/app-debug.apk

# iOS Configuration (если нужно)
IOS_PLSimilar_VERSION=17.0
IOS_DEVICE_NAME=iPhone 15 Pro
IOS_APP_PATH=../build/iphone/FoodApp.app

# Test User
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=Test123456
TEST_USER_NAME=Test User

# Инструкция:
# 1. Скопируйте этот файл в .env: copy env.example .env
# 2. Для физического устройства выполните:
#    adb devices -l  # чтобы узнать device id
#    adb shell getprop ro.build.version.release  # чтобы узнать версию Android
# 3. Укажите полученные значения в .env

