# Запуск диагностических тестов

Эти тесты помогут понять, какие локаторы работают в вашем окружении.

## Запуск

### Запустить все диагностические тесты:
```bash
cd e2e_tests
python -m pytest tests/test_element_finding.py -v -s
```

### Запустить конкретный тест:

**1. Полная проверка всех способов поиска:**
```bash
python -m pytest tests/test_element_finding.py::test_check_all_element_locators -v -s
```

**2. Проверка текущих локаторов:**
```bash
python -m pytest tests/test_element_finding.py::test_check_current_locators -v -s
```

**3. Практический пример:**
```bash
python -m pytest tests/test_element_finding.py::test_practical_demo -v -s
```

## Что покажут тесты

### test_check_all_element_locators
Проверит ВСЕ возможные способы поиска:
- testID через By.ID
- testID через AppiumBy.ACCESSIBILITY_ID
- XPath по тексту
- XPath по hint
- И другие варианты

Для каждого способа покажет:
- ✅ FOUND - элемент найден
- ❌ NOT FOUND - элемент не найден
- Видимый ли элемент
- Активен ли элемент

### test_check_current_locators
Проверит локаторы из `sign_in_page.py`:
- LOGIN_BUTTON
- EMAIL_INPUT
- PASSWORD_INPUT
- и другие

Покажет, какая из стратегий (fallback) работает.

### test_practical_demo
Демонстрирует использование работающих локаторов:
- Ввод email
- Ввод пароля
- Проверка загрузки страницы

## Интерпретация результатов

### ✅ Что работает:
```
✅ hint contains email: FOUND
✅ clickable with child text XPath: FOUND
```
→ Используйте эти стратегии

### ❌ Что не работает:
```
❌ testID By.ID: NOT FOUND
❌ testID AppiumBy.ACCESSIBILITY_ID: NOT FOUND
```
→ Избегайте этих стратегий (или добавьте fallback)

## Пример вывода

```
🔍 Проверяем кнопку 'Войти':
------------------------------------------------------------
❌ testID By.ID: NOT FOUND
❌ testID AppiumBy.ACCESSIBILITY_ID: NOT FOUND
✅ clickable with child text XPath: FOUND
   Видимый: True, Активен: True, Текст: Войти

РЕКОМЕНДАЦИИ:
✅ Используйте стратегии, которые вернули FOUND
❌ Избегайте стратегии, которые вернули NOT FOUND
```

Это покажет, какие локаторы реально работают в вашем случае!

