// Export individual theme elements for direct imports
export { colors, darkColors, getThemeColors } from './colors';
export { typography, fontFamilies, fontSizes, lineHeights, fontWeights } from './typography';
export { spacing, componentSpacing, borderRadius, shadows } from './spacing';

// Export theme objects
export { baseTheme, darkTheme, lightTheme, defaultTheme } from './theme';
export type { Theme, ThemeContextType } from './theme';

